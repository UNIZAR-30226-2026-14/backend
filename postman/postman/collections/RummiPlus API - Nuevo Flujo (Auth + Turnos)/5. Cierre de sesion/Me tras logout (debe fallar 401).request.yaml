$kind: http-request
url: "{{baseUrl}}/api/auth/me"
method: GET
headers:
  Authorization: Bearer {{tokenJugador1}}
scripts:
  - type: afterResponse
    code: pm.test('Status 401', function () { pm.response.to.have.status(401); });
    language: text/javascript
order: 2000
