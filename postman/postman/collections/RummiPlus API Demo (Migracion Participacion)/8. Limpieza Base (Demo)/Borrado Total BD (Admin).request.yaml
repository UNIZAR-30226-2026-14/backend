$kind: http-request
url: "{{baseUrl}}/api/admin/wipe"
method: DELETE
scripts:
  - type: afterResponse
    code: |-
      pm.test("Wipe total devuelve 204", function () {
        pm.response.to.have.status(204);
      });
    language: text/javascript
order: 1000
